<% provide(:title, "Home") %>

<div class='hero-unit'>
  <h1>Flow <span style="font-size:50%">Twitter like app built with rails</span></h1>
  <br>
  <p class="lead">
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name),html:{style:'display:inline;'}) do |f| %>
      <%= f.hidden_field :email, value:'guest@example.com' %>
      <%= f.hidden_field :password, value:'password' %>
      <%= f.submit "Preview", class:"btn" %>
    <% end %>
    the app with the guest account. All actions performed by guest account will be removed periodically.
  </p>
  <p>
    <a href="#sign_up_modal" role="button" class="btn btn-large btn-primary" data-toggle="modal" style="font-size: 140%;">Sign up</a>
  </p>
  Already have an account?
  <a href="#sign_in_modal" data-toggle="modal">Sign in here</a>
</div>

<!-- sign up modal form -->
<%= render 'static/sign_up_modal' %>

<!-- sign in modal form -->
<%= render 'static/sign_in_modal' %>